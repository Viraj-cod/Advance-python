def oper(func,x,y):
    return func(x,y)
    
def add(a,b):
    return a+b

def multi(a,b):
    return a*b

m = int(input("Enter 1st no:"))
n = int(input('Enter the second no:'))
print(oper(add,m,n))

ave = lambda x,y:f'Average of two numbers:{(x+y)/2}'
print(ave(m,n))
#using git saving in local 
